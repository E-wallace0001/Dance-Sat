if propagated, follow 


if 0 was not allowed, go back throgh the clause that allowed 0 to not be allowed, look to change it

	---> if set variable. break this loop
	

Create list
	 |
	 ---> append Causal / inital neg
	 |
	 ---> Search through clause looking for members of list
		 |
		 ---> Look for 0 set variable
		 |		|
		 |		---> if preset, do nothing
		 |
		 ---> if preset, do nothing
	|
	|
	--->if no more clauses, this is unsat 		
		 		
		
 
